use std::path::Path;

use laml::laml2yaml;

fn main(path: &Path) {
    // if file: transpile check is laml, transpile to yaml
    // if dir: transpile all lamls into yamls
    todo!()
}

fn transpile(file: &Path) {
    // laml = read file.laml
    // yaml = laml2yaml(laml)
    // write file.yaml
    todo!()
}
